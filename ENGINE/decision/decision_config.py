# QuantOS Decision Engine V10 Configuration

# Constants and default values for Decision Engine V10

# Example: Default thresholds (if any are needed and not defined elsewhere)
DEFAULT_CONFIDENCE = 0.7
DEFAULT_DECISION_SCORE = 0.5
DEFAULT_UNCERTAINTY = 0.3

# Example: Default reasons
DEFAULT_REASONING = ["Default reasoning applied."]
DEFAULT_BLOCKING_REASONS = ["Default blocking reason."]
DEFAULT_SUPPORTING_REASONS = ["Default supporting reason."]
DEFAULT_EVIDENCE_USED = ["Default evidence."]
DEFAULT_MODULES_CONSULTED = ["Default modules."]

# Versioning (can be overridden by DecisionContext/Result directly if needed)
VERSION_ARCH = "V10.0"
VERSION_CONTRACTS = "V10.0"
